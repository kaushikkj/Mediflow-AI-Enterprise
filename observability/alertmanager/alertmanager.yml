global:
  resolve_timeout: 5m

route:
  receiver: mediflow-local
  group_by:
    - alertname
    - job
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: mediflow-local